spring:
  cloud:
    nacos:
      config:
        server-addr: 47.83.162.65:8848
        username: nacos
        password: nacos
    gateway:
      discovery:
        locator:
          enabled: true
        routes:
          - id: auth-service
            uri: lb://auth-service